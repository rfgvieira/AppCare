<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EstudoCompose" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
            <!-- Status bar color. -->
            <item name="android:statusBarColor" >@color/blue_medium</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.EstudoCompose.NoStatus" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EstudoCompose.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EstudoCompose.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>